/**
 * @format
 */

import {AppRegistry} from 'react-native';
import DateTimePickerModal from './App';
import {name as appName} from './app.json';


AppRegistry.registerComponent(appName, () => DateTimePickerModal);
